#include <math.h>
#include <stdio.h>
int func(int a);
float x;
int main() {
    char j;
    if printf("n/a");

    else {
        float v1 = 7e-3 * pow(x, 4.0);
        float v2 = ((22.8 * pow(x, 1.0 / 3.0) - 1e3) * x + 3.0);
        float v3 = (x * x / 2.0);
        float v4 = (x * pow(10.0 + x, 2.0 / x));
        float itog = v1 + v2 / v3 - v4 - 1.01;
        printf("%.1f\n", itog);
    }
}
